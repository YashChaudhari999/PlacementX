/**
 * Shared Type Definitions
 *
 * Export all shared TypeScript types and interfaces from this barrel file.
 * These types are designed to be reusable across web and mobile (React Native) platforms.
 *
 * @module shared/types
 */

export {};
